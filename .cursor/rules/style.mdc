---
alwaysApply: true
---

# Convenciones de Estilo y Código

## 🌐 Lenguaje

- **Código**: Inglés (variables, funciones, comentarios técnicos)
- **Contenido**: Español (proyectos, UI, traducciones)
- **Documentación**: Español (docs, README, guías)
- **Commits**: Español preferido

## 📝 TypeScript

- Usar strict mode
- Definir tipos explícitos para props
- **NO usar `any`**, preferir `unknown` o tipos específicos
- Interfaces para objetos, types para uniones/alias

```typescript
// ✅ Correcto
interface Props {
  title: string;
  count: number;
}

// ❌ Incorrecto
interface Props {
  title: any;
  count: any;
}
```

## ⚛️ React

- Componentes funcionales con TypeScript
- Props interface definida
- `"use client"` para interactividad
- Preferir composición sobre herencia

```tsx
"use client";

interface ButtonProps {
  label: string;
  onClick: () => void;
}

export default function Button({ label, onClick }: ButtonProps) {
  return <button onClick={onClick}>{label}</button>;
}
```

## 🚀 Astro

- Usar TypeScript en frontmatter
- Props interface exportada
- Componentes puros cuando sea posible
- Client directives apropiadas (`client:load`, `client:visible`)

```astro
---
export interface Props {
  title: string;
}

const { title } = Astro.props;
---

<h1>{title}</h1>
```

## 🎨 Tailwind CSS

- **Usar v4 syntax** (sin `@apply`)
- Clases ordenadas: layout → spacing → colors → effects
- Usar variables CSS para tema
- Responsive-first approach

```tsx
// ✅ Correcto
<div className="flex items-center gap-4 px-6 py-4 bg-primary text-white rounded-lg hover:bg-primary/90">

// ❌ Incorrecto (orden desordenado)
<div className="text-white px-6 bg-primary gap-4 flex py-4 rounded-lg items-center">
```

## 📦 Imports

- Usar path aliases
- Ordenar imports: externos → internos → locales
- Agrupar por tipo

```typescript
// ✅ Correcto
import { useState } from "react";
import { Button } from "@/components/ui/button";
import { useTranslations } from "@/i18n/utils";

// ❌ Incorrecto
import { useTranslations } from "../../i18n/utils";
import { useState } from "react";
import { Button } from "../ui/button";
```

## 🎯 Naming Conventions

### Variables y Funciones

```typescript
// camelCase para variables y funciones
const userName = "John";
function getUserName() {}

// PascalCase para componentes y clases
function UserProfile() {}
class UserManager {}

// UPPER_CASE para constantes
const API_URL = "https://api.example.com";
```

### Archivos

```
ComponentName.astro    # PascalCase para componentes
ComponentName.tsx      # PascalCase para componentes React
feature-name.ts        # kebab-case para utilidades
nombre-proyecto.mdx    # kebab-case para contenido
```

## 🧹 Linting y Formateo

### Antes de Commit

```bash
npm run lint     # ESLint
npm run format   # Prettier
npm run build    # Verificar que compila
```

### Rules

- Sin console.log en producción
- Sin variables sin usar
- Sin imports sin usar
- Usar const/let, nunca var

## 🔍 Accesibilidad

- Usar semantic HTML
- Incluir ARIA labels cuando sea necesario
- Alt text en todas las imágenes
- Teclado navegable

```tsx
// ✅ Correcto
<button aria-label="Cerrar menú" onClick={close}>
  <X />
</button>

// ❌ Incorrecto
<div onClick={close}>
  <X />
</div>
```

## 🔗 Referencias

- **Stack completo**: `/docs/architecture/tech-stack.md`
- **Documentación**: `/docs/README.md`